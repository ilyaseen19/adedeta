import {StyleSheet, Dimensions} from 'react-native';

const styles=StyleSheet.create({
    imageBackground: {
        flex: 1,
        width: null,
        height: null,
        alignItems: "center"
    }
});

export default styles;